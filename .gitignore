.venv/
__pycache__/
*.pyc
.DS_Store

# Firebase CLI local state (rules/firebase.json ARE committed; this is runtime cruft)
.firebase/
firebase-debug.log

# Per-verse pitch shards are generated by scripts/split_pitch.py and not yet
# fetched at runtime (the app uses the *_pitch.slim.json / *_pitch.raw.json
# monoliths). Regenerate them when phase-2 lazy per-verse loading lands.
data/pitch/
